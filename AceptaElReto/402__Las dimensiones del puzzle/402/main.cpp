// AceptaElReto 402:  Las dimensiones del puzzle
#include <algorithm>
#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <stdio.h>
#include <math.h>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int size;
    int i;
    while(cin>>size && size){
        for(i=sqrt(size);i>=;i--){
            if(!(n%i)){
                cout<<n/i<<"\n";
                break;
            }
        }
        
    }
    return 0;
}

